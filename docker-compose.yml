version: '3.10'
services:
  crowdfunding:
    build: .
    ports:
      - "8085:8080"
    depends_on:
      - db
  db:
    image: mysql:8.0.30
    environment:
      - MYSQL_DATABASE=${MYSQL_DATABASE_URL}
      - MYSQL_ALLOW_EMPTY_PASSWORD=1
    ports:
      - "3307:3306"

